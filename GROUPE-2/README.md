# GROUPE-2 — Livrables à envoyer

Destinataire : **nds.ucad.fst.lacgaa@gmail.com**

## Contenu de ce dossier

- `Rapport_PKI_EJBCA_GROUPE-2.docx` — rapport détaillé (description, installation, configuration,
  tests). **Les captures d'écran sont en placeholders** (`[ Insérer capture d'écran ici : ... ]`) :
  à remplacer par vos propres captures au fur et à mesure de la manipulation (voir
  `../docs/guide-ejbca.md` et `../docs/guide-https.md` pour la liste des étapes correspondantes).
- `Presentation_PKI_EJBCA_GROUPE-2.pptx` — support de présentation résumant le projet (mêmes
  placeholders de captures à remplacer).
- `VIDEO/script-video-demo.md` — script/plan pour enregistrer la vidéo de démonstration.
- `VIDEO/demo-pki-ejbca-groupe-2.mp4` — **à ajouter** une fois la vidéo enregistrée.

## Équipe

- **Ahmad Diop** — branche `pki-ejbca-setup` (déploiement EJBCA, Root CA, certificat serveur)
- **Papa Mamadou** — branche `webserver-https-migration` (serveur web, migration HTTPS)
- **Mame Fama** — branche `docs-rapport-tests` (rapport, tests, documentation)

## À faire avant l'envoi

1. ~~Remplacer les noms~~ — déjà fait (Ahmad Diop, Papa Mamadou, Mame Fama).
2. Effectuer réellement les manipulations (installation EJBCA, configuration CA, migration
   HTTPS, tests) en suivant les guides du dépôt (`docs/guide-ejbca.md`, `docs/guide-https.md`),
   et prendre les captures d'écran demandées à chaque emplacement `[ Insérer capture d'écran ]`.
3. Compléter la section « Difficultés rencontrées » (rapport + PPT) avec les vrais problèmes
   rencontrés par l'équipe.
4. Enregistrer la vidéo de démonstration en suivant `VIDEO/script-video-demo.md`, l'exporter en
   MP4 et la placer dans `VIDEO/`.
5. Vérifier la taille totale du dossier (compresser la vidéo si nécessaire pour l'envoi par email,
   ou utiliser un lien de partage si la pièce jointe est trop volumineuse).
6. Envoyer le dossier `GROUPE-2` (zippé si besoin) à `nds.ucad.fst.lacgaa@gmail.com`.
